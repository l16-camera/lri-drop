[package]
name = "lri-drop"
version.workspace = true
edition.workspace = true
description = "Drag-and-drop LRI → DNG converter — isamarin × BLMK"

[lib]
name = "lri_drop_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2.6", features = [] }

[dependencies]
# Point at luminat monorepo (see README / scripts/link-luminat.sh)
light = { path = "../vendor/luminat/light" }
tauri = { version = "2.6", features = [] }
tauri-plugin-dialog = "2.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
camino = "1.1"
anyhow = "1"
png = "0.17"
